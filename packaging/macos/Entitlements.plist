<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Microphone access is still mediated by TCC and the purpose string in
         Info.plist. The app does not disable hardened-runtime library
         validation and does not request an undocumented system-audio
         entitlement; CoreAudio process-tap consent uses
         NSAudioCaptureUsageDescription. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
